{
  "schemaVersion": 1,
  "repository": "FuzzySlipper/rusty-dagger",
  "requiredChecks": ["ci"],
  "localVerification": [
    "./scripts/verify.sh"
  ],
  "notes": "Managed reviews require the active C# product CI: safe build, focused tests, DOM UI build, and NativeAOT publish. Add gates only when an active product concern requires them."
}
